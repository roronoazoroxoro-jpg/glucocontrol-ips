{
  "buildCommand": "npm install && npx prisma generate && npx prisma db push && npm run build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": "file:/tmp/glucocontrol.db",
    "USDA_API_KEY": "DEMO_KEY"
  }
}
